

{# resources/views/themes/classic/index.twig أو modern/index.twig #}

{% for section in page.sections %}
    {% if section is iterable %}
        {% set type = section.type %}
        {% include "themes/" ~ page.theme ~ "/sections/" ~ type ~ ".twig" with section %}
    {% endif %}
{% endfor %}